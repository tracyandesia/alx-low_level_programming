#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: always 0
 */


int main(void)
{
	int a, b;

	for (a = 48; n <= 56; a++)
	{
		for (m = 49; b <= 57; m++)
		{
			if (b > a)
			{
				putchar(a);
				putchar(b);
				if (a != 56 || b != 57)
				{
					putchar(',');
					putchar(' ');
				}
			}
		}
	}
	putchar('\n');
	return (0);
}

